# Local development: everything the pipeline needs, plus the dashboard,
# notebooks and the Pub/Sub module. Shared pins live in requirements-pipeline.txt
# so they cannot drift - an imbalanced-learn mismatch between these two files
# once produced a venv that could not import sklearn.
-r requirements-pipeline.txt

beautifulsoup4==4.14.3
google-auth==2.49.2
google-cloud-pubsub==2.38.0
lxml==5.3.0
matplotlib==3.10.8
notebook==7.5.6
streamlit==1.58.0
